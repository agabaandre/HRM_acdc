{
  "name": "africa-cdc-staff-portal",
  "private": true,
  "version": "0.1.0",
  "description": "Orchestrate Laravel API (root/public) + Vue SPA (frontend/) — same pattern as helpdesk",
  "scripts": {
    "install:all": "composer install && cd frontend && npm install --cache ./.npm-cache --legacy-peer-deps",
    "dev:api": "php artisan serve --host=127.0.0.1 --port=8081",
    "dev:web": "cd frontend && npm run dev",
    "dev:all": "npx concurrently -c \"#119a48,#0d7a3a\" \"npm run dev:api\" \"npm run dev:web\" --names=api,web"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
