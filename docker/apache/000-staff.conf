<VirtualHost *:80>
    ServerAdmin webmaster@localhost
    ServerName localhost

    # CodeIgniter staff portal (repo root)
    DocumentRoot /var/www/html

    <Directory /var/www/html>
        DirectoryIndex index.php index.html
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Laravel APM — must match APP_URL path (e.g. http://localhost:8080/apm)
    Alias /apm /var/www/html/apm/public

    <Directory /var/www/html/apm/public>
        DirectoryIndex index.php index.html
        Options FollowSymLinks
        AllowOverride All
        Require all granted
    </Directory>

    # Show errors in `docker compose logs web` (Debian Apache in container)
    ErrorLog /proc/self/fd/2
    CustomLog /proc/self/fd/1 combined
    LogLevel warn
</VirtualHost>
